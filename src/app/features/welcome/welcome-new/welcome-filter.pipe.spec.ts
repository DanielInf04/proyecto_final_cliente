import { WelcomeFilterPipe } from './welcome-filter.pipe';

describe('WelcomeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WelcomeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
